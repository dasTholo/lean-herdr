# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# uv
uv.lock

# Test / tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/

# LMD skills are installed artefacts, reproducible via:
#   lean-md skill install <name> --local
.claude/skills/

# machine-local, written by `lean-herdr models` -- never shared
.lean-ctx/lean-herdr/models.auto.toml

# temp files of the overlay and template-lock writers, left only by a hard kill
.lean-ctx/lean-herdr/.tmp-*
